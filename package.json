{
  "name": "facelipa",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "download-models": "npm run download-models --prefix frontend"
  }
}
